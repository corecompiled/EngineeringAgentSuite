# Memory — ado-pr-reviewer

Persistent state for this agent. Loaded every session. Update per shared conventions §3.

## MCP Setup

- Server: (not yet configured — run first-run bootstrap)
- Configured in: (workspace `.kiro/settings/mcp.json` | global `~/.kiro/settings/mcp.json`)
- Tool-name patterns observed:
  - list PRs / reviewer filter: —
  - fetch PR details/threads: —
  - fetch diff / file contents: —
  - fetch linked work items: —

## User Identity

- ADO display name: (ask on first run)
- ADO email: (ask on first run)

## Local Repo Clones

(repo name → local path; ask when first useful)

## Learned Preferences

(none yet — e.g. severity thresholds, comment tone)

## Changelog

- 2026-08-14 — memory file created with the initial scaffold.

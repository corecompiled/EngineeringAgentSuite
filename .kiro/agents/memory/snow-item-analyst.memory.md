# Memory — snow-item-analyst

Persistent state for this agent. Loaded every session. Update per shared conventions §3.

## MCP Setup

- Server: (not yet configured — run first-run bootstrap)
- Configured in: (workspace `.kiro/settings/mcp.json` | global `~/.kiro/settings/mcp.json`)
- Tool-name patterns observed:
  - fetch record fields: —
  - fetch activity stream (work notes vs comments): —
  - fetch related records / catalog variables: —
  - query/search records: —

## Known Record Prefixes

(company-specific prefixes learned from use, e.g. Case numbers: —)

## Learned Preferences

(none yet — e.g. preferred tone/sign-off for client-facing drafts)

## Changelog

- 2026-08-14 — memory file created with the initial scaffold.

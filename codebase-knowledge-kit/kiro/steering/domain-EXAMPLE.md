---
inclusion: fileMatch
fileMatchPattern: "REPLACE-repo-or-path/**"
---

# Domain Steering — EXAMPLE (copy per major module, rename, set the pattern)

When working under this path, this module's deep documentation applies:

- Architecture doc: `~/Documents/Engineering Knowledge Base/architecture/REPLACE-module.md` — read it before proposing changes here.
- API dump: `docs/api-surface/projects/REPLACE-Project.md`
- Owner/notes: TODO (team, on-call, review requirements)

Module-specific rules (examples — replace):
- TODO e.g. "All money amounts are decimal; never float."
- TODO e.g. "Public methods here are consumed by external clients — signature changes need a deprecation cycle (see contracts doc)."
